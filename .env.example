# Nothing is required for the digest to work - it uses public RSS feeds
# and a free local summarizer (no API key, no signup).

# Optional: only set this if you also want the digest posted to a Discord
# channel (in addition to the markdown file it always writes to the repo).
DISCORD_WEBHOOK_URL=your_discord_webhook_url_here

# Optional
DEBUG=false
